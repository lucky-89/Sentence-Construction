{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "json-server db.json --port 4000 --no-static"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
